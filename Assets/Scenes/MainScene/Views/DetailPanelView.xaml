<UserControl x:Class="Feudal.Scenes.Main.Views.DetailPanelView"
             Name="DetailPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Feudal.Scenes.Main"
             xmlns:views="clr-namespace:Feudal.Scenes.Main.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="40"/>
            <RowDefinition MaxHeight="40"/>
            <RowDefinition MaxHeight="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Column="0" Grid.RowSpan="4">
            <ContentPresenter Content="{Binding CurrentView, ElementName=DetailPanel}" />
        </Grid>
        <Button Grid.Column="1" Grid.Row="0" Command="{Binding PrevPanel}" >&lt;</Button>
        <Button Grid.Column="1" Grid.Row="1" Command="{Binding NextPanel}" >&gt;</Button>
        <Button Grid.Column="1" Grid.Row="2" Command="{Binding ClosePanel}" >X</Button>
        <Grid Grid.Column="2" Grid.RowSpan="4">
            <ContentPresenter Content="{Binding SubView, ElementName=DetailPanel}" Visibility="{Binding Path=SubViewModel, Converter={StaticResource NullToVisibilityConverter}}"/>
        </Grid>
        <Button Grid.Column="3" Command="{Binding CloseSubPanel}" Visibility="{Binding Path=SubViewModel, Converter={StaticResource NullToVisibilityConverter}}" >X</Button>
    </Grid>
</UserControl>
